name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'zulu'

      # 1. تثبيت pnpm أولاً (هذا هو التعديل المطلوب)
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 8

      # 2. الآن يمكن إعداد Node.js واستخدام pnpm للـ cache
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Android app
        run: |
          cd tracker-v2/device-tracker-v2/android
          chmod +x gradlew
          ./gradlew assembleRelease

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: tracker-v2/device-tracker-v2/android/app/build/outputs/apk/release/*.apk
          
