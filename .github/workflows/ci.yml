name: CI / Build Android APK (Capacitor)

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22          # مطلوب لـ Capacitor CLI >=22
          cache: pnpm

      - run: pnpm install

      # بناء مشروع الويب (إذا كان لديك script build)
      - run: pnpm build

      # تأكد من وجود Capacitor CLI (إذا لم يكن في package.json)
      - name: Ensure Capacitor CLI is installed
        run: pnpm add -D @capacitor/cli

      # تثبيت حزمة Android platform
      - name: Install Capacitor Android platform
        run: pnpm add @capacitor/android

      # مزامنة Capacitor مع Android
      - name: Sync Capacitor with Android
        run: pnpm exec cap sync android

      # إعداد Java وبناء Android
      - name: Setup Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Build Android app
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: android/app/build/outputs/apk/release/app-release.apk
          if-no-files-found: error
