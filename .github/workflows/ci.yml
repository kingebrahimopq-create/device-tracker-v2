steps:
  - uses: actions/checkout@v4

  # تثبيت ppnpm حسب الإصدار المحدد في package.json (دون تحديد version)
  - uses: pnpm/action-setup@v4

  - uses: actions/setup-node@v4
    with:
      node-version: 20
      cache: pnpm

  - run: pnpm install
  - run: pnpm build
  - run: pnpm test
